import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'in-app-purchase-2-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
